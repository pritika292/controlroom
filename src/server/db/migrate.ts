// Migration runner placeholder -- full implementation lands in issue #3.
// This file exists so `npm run migrate` and the Dockerfile CMD succeed
// before the DB layer is wired up.
console.log("[migrate] no migrations to run yet");
